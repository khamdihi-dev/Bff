    $ pkg update && upgrade
    $ pkg install python
    $ pkg install git
    $ pip install rich
    $ pip install requests
    $ git clone https://github.com/khamdihi-dev/bff
    $ cd bff
    $ python main.py

    * minimal di follow/kasih bintang🙄
